---
import MainLayout from "../layouts/MainLayout.astro";

// const foregroundColor = "hsl(20, 50%, 50%)";
// const backgroundColor = "hsl(200, 50%, 50%)";

const isRed = false;
---

<MainLayout title="about" description="Added a description...">
  <p
    class:list={[{ red: isRed }]}
    class="ml-3 mt-2 mb-3 font-light text-gray-500 dark:text-gray-400"
  >
    Not sure if this will show...
  </p>
  <p class="big ml-3 mt-2 mb-3 font-light text-gray-500 dark:text-gray-400">
    Not sure if this will show...
  </p>
</MainLayout>

<!-- <style define:vars={{ foregroundColor, backgroundColor }}>
  p {
    background-color: var(--backgroundColor);
    color: var(--foregroundColor);
  }
</style> -->
<style>
  .big {
    font-size: 4rem;
  }
  .red {
    color: rgb(248 113 113);
  }
</style>
